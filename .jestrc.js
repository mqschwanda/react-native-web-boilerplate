const config = {
  testURL: 'http://localhost',
  testRegex: '.*.test.js$',
  preset: 'react-native',
  verbose: true,
  moduleDirectories: [
    'node_modules',
  ],
};

// config.verbose = false;

module.exports = config;
